{
  "name": "@huntbound/workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24.14.0 <25"
  },
  "scripts": {
    "dev": "corepack pnpm --filter @huntbound/game dev",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "typecheck": "corepack pnpm --recursive run typecheck",
    "test": "vitest run && node --test tools/architecture/check-boundaries.test.ts && corepack pnpm --recursive run test",
    "build": "corepack pnpm --recursive run build",
    "architecture:check": "node tools/architecture/check-boundaries.ts",
    "diagnostics:boot-stall": "node tools/diagnostics/runBootStallMatrix.ts",
    "check": "biome check . && corepack pnpm typecheck && corepack pnpm test && corepack pnpm architecture:check",
    "qa:browser": "corepack pnpm build && playwright test",
    "verify": "corepack pnpm format:check && corepack pnpm architecture:check && corepack pnpm typecheck && corepack pnpm test && corepack pnpm build && corepack pnpm qa:browser"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@playwright/test": "1.62.1",
    "@types/better-sqlite3": "7.6.13",
    "@types/node": "24.13.3",
    "better-sqlite3": "13.0.3",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "4.1.10"
  }
}
